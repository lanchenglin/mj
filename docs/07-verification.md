# v0.2 实际验证记录

日期：2026-09-16。只记本次有实际执行结果的检查，不沿用此前没有文件支持的“37 项通过”说法。

## 已执行

- `python -m pytest --disable-warnings`：51 项通过。范围是认证/CSRF/所有权、版本冲突、任务幂等、预算串行准入与并发测试、取消/未知提交/晚到结果、媒体导入、真实 FFmpeg 小片段渲染、中文断句、导出、供应商请求契约桩测试。
- `node --check mj/web/app.js`：语法检查通过。
- Alembic 初始迁移：在隔离 SQLite 测试库实际执行 upgrade head→downgrade base→upgrade head；不是 PostgreSQL 测试结果。
- `scripts/browser_check.py`：Chromium 实际执行登录、创建演示项目、修改并保存剧本、保存角色设定、查看分镜、手机布局、提交完整预演任务。无 JS pageerror，390px 宽度没有页面横向溢出。
- 浏览器直接 HTTP 在本环境被网络策略限制，因此使用明确标记的进程内 TestClient 测试桥；没有修改浏览器/管理员的网络安全策略。此结果不替代部署后的 HTTP、TLS、Cookie、CSP 联调。
- `scripts/verify_120s.py`：真正运行 FFmpeg 合成 20 个工程占位镜头，360×640、30 fps，解码 3600 帧，PCM 音轨 5,760,000 采样。输出有 MOCK 标识，所有 paid_calls 为 0；不是正式原创漫剧样片。

## 实际修复

完整 120 秒工程测试发现：只设置分镜输入帧率与帧数时，片段封装 duration 少一帧，concat 后字幕重编码丢帧。现将分段滤镜时间基设为 1/fps、PTS=N，输出显式 `-r` 与 CFR。补充多片段时间戳回归，同时校验解码帧数、视频实际 duration 和 PCM 采样数；没有用强制截断成片来掩盖问题。

## 未验证，不应宣称通过

真实外部生成模型/账户权限/中文声音/角色一致性/口型/真实账单；PostgreSQL 并发与故障恢复；Docker 镜像构建与部署；原生 Windows/macOS；完整公网安全、出站策略和生产灾备；原始大纲全部 T01–T24 的所有边界；真实 G3/G4 创作验收。

已运行的供应商测试采用本地请求桩，没有真实联网或扣费。不要把 51 项回归测试等同于“工业化验收全部通过”。

## Git 状态

完整 v0.2 源码已成功写入 `rebuild/original-drama-v0.2`。实现提交 `e99db5e1e8cec5bf03b0904628553c29dd34660b` 包含 Web 工作台、部署配置、迁移、验证脚本与 51 项测试；远端递归树已回读确认这些文件实际存在。此前仅有后端检查点的说明已经撤下。

Git 发布是否等于真实漫剧质量通过：**不是**。本节只说明源码已发布，前述真实模型、PostgreSQL/Docker、生产安全和 G3/G4 创作验收仍保持“未验证”。
