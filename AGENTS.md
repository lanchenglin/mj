# MJ 开发约束

## 事实与入口

先读 README、docs/06-implementation.md、docs/07-verification.md。仓库旧 docs/00–05 是目标设计，不是全部已实现功能。v0.2 的源代码在 mj/，API 与 Worker 分离，网页在 mj/web/。

## 不可绕过的边界

- 优先原创漫剧，不扩展影视解说、自动发布、SaaS 或无限画布。
- 外部调用默认关闭，模拟只能测试工程，不能伪装真实模型结果。
- 人物动作不可静默降级为静图运动；未实现的口型/特效明确阻塞。
- 版本不可覆盖，候选不会自动应用，审批绑定具体内容与素材。
- 未知提交先核对，不换请求身份盲目重试 POST；取消不假设退款。
- 预算与审批来自已认证的用户，不由模型自行批准。
- 原素材不删改，密钥、数据库、生成媒体、日志不提交 Git。
- FFmpeg 参数来自受限代码，不能执行模型生成的 shell/filter/程序。

## 工程

生产仅 PostgreSQL，SQLite 是离线演示/测试。迁移不可通过修改已发布的 0001 实现升级，要新增迁移。修改数据契约要同步前端和测试。当前前端为原生 ES modules；迁回 React/TypeScript 需独立决策，不能声称现在已经采用。

每次交付运行 pytest、JS 语法检查，并在可用环境检查浏览器与真实 FFmpeg。报告实际命令、测试范围、失败与未验证项。51 项开发回归不等于所有原始验收计划已完成。未获付费和外发授权不得跑真实模型测试。

推送前回读目标分支，不强推、不覆盖并发提交，不把本地提交或 Git tree 对象说成已更新远端分支。工具写入若被安全检查拦截，不更换路径、编码或工具绕过；保留可验证的本地源码包与已成功远端提交。
