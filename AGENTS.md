# MJ 开发与交接约束

## 当前状态

本仓库目前只有原创漫剧的设计文档与规划样例，没有已实现的后端/前端、数据库、模型接口或成片。先读 README，再按 docs/00–05 理解范围。不要把目录示意、接口大纲或测试计划当成已有实现。

## 实施顺序

按 docs/04 的 M1→M4 和 MJ-001 等任务推进，先完成可运行的最小纵向闭环与测试。先做原创漫剧，不顺便增加原片解说、自动发布、多租户计费或无限画布。新需求进入决策记录，不通过改提示词偷偷改变产品目标。

## 不可绕过的边界

- 付费默认关闭；真实文本/视觉/图像/视频/TTS 调用都需要预算与外发范围授权。dry-run 不访问付费生成服务。
- 工具成功不等于镜头通过，镜头通过不等于整集可交付。模拟素材必须明显标识。
- 人物动作合同不能用静图推拉冒充；不支持能力应阻塞并说明，不删除字段“兼容”。
- 保留不可变资产版本、具体审批版本、输入指纹和远端 submission 身份；晚到结果不覆盖当前方案。
- 未知提交先查单；没有幂等/查单能力时停住核对，不能盲目重试收费任务。
- 预算和审批由服务端判断，Agent 不能给自己批准、改账本或获取任意 shell/密钥权限。
- 原始媒体只读；失败不删除原素材；永久删除和公开发布需要单独授权。

## 代码与上游

选择性复用允许复用的代码，记录 commit/路径/许可证/本地修改。不要直接复制 AGPL 或带补充商业限制的平台代码进入当前主项目，也不要抹去 MIT 等来源声明。MJ 自有代码 LICENSE 等待仓库所有者选择。

依赖与外部接口在实际接入时核对官方版本和安全状况，并锁定可复现版本。禁止硬编码真实 API Key、用户素材路径或价格。所有生成媒体、数据库、日志和凭据留在 Git 之外。

## 验收与报告

依据 docs/04 的 T01–T24 补测试。每次修改报告实际运行的命令与结果，未运行就写未运行；不能用“应该通过”当实测。模型效果与费用必须来自实际授权样片，不复制宣传报价。

`examples/120s-storyboard.plan.json` 只验证规划结构；禁止直接当获批生产请求执行。文档更新同时检查相对链接、JSON、镜头区间和状态说明的一致性。

推送前检查仓库现有变更，不强推、不覆盖并发提交。遇到接口或设计冲突，先记录冲突与迁移方案，保留原文件和历史。
