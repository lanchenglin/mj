# v0.2 重写交付状态 · 2026-09-16

本分支已实际保存后端核心代码，核心提交为 `1773b4c94315c22383d6c6cc3c1f66f88594a78d`，包含工作区数据契约、项目版本、审批、预算、生成队列、Worker、模型适配、认证 API 和 FFmpeg 合成。

## 重要：本分支不是完整可部署版本

随后上传网页源码的工具请求被平台安全检查拦截。本次没有更换路径、编码或工具绕过拦截，也没有将不完整应用更新到 main。网页、部署、测试和完整运行文档存在于当前交付源码包中，但尚未全部写入此远端分支。仅检出本后端检查点无法直接运行 Web 工作台。

## 在完整本地实现上实际执行的验证

- pytest：51 项通过，覆盖领域逻辑、任务/预算、媒体与供应商请求桩；不表示原始大纲全部工业化验收通过。
- JavaScript 语法检查通过。
- SQLite 隔离库执行 Alembic upgrade → downgrade → upgrade；未运行 PostgreSQL 集成验证。
- Chromium 实际完成登录、创建演示项目、保存剧本/设定、分镜与手机布局、提交预演任务。测试使用进程内 TestClient 桥接，不是已通过真实部署 HTTP/CSP 联调。
- FFmpeg 实际生成完整 120 秒工程预演：360×640、30 fps、3600 个解码帧、PCM 音轨 5,760,000 采样；实际视频 duration=120.0 秒。全片有 MOCK 标记，付费调用为 0。

上述测试针对本地完整源文件，不应称为本分支或 GitHub Actions 已通过同样测试。真实生成模型、角色一致性、动作、中文音色、真实费用、PostgreSQL/Docker 和生产部署尚未验收。

完整源码包以本轮聊天实际提供的 ZIP 为准。保留原设计文档，不将工程占位预演称为已生成的正式漫剧。
